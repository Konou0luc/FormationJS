// Different type de DOM Document Object Model

    // getElementsByTangeName

let p = document.getElementsByTagName("p"); // recuperation d'une balise 

    // getElementsByClassName
var h1 = document.getElementsByClassName("h1"); // recuperation d'une classe

    // getElementsById

var header = document.getElementById("header"); // recuperation d'un id

let h1 = document.querySelector(".h1"); // recuperation d'une balise h1

// # pour recuperer un id
// le . pour recuperer une classe
// appel d'une balise

querySelectorAll(".h1"); // recuperation de tous les elements qui ont la classe h1
